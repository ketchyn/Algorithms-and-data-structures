package All;

/**
 * Created by Alexandr on 20.12.2015.
 */
public class cunt1 {

    /*static  int buff = 0;
     static int max_size = 0;
    static int min_size = 0;
    static int[] arrMax;
    static int[] arrMin;
    public static void main(String[] args) {
        int[] AA = {9,9,9,9};
        int[] b = {9,9,9};


        if(AA.length>=b.length){
            max_size = AA.length;
            min_size = b.length;
            Arrmax = new int [max_size];
            System.arraycopy(AA,0,arrMax,0,arrMax.length);
            System.arraycopy(b,0,arrMin,0,arrMax.length);

        } else {

            max_size = b.length; min_size = AA.length;
            arrMax = new int [max_size];
            System.arraycopy(b,0,arrMax,0,arrMax.length);
            System.arraycopy(AA,0,arrMin,0,arrMax.length);



        }
        //Integer[] res = new Integer[max_size+1];
        int[] res = new int[max_size+1];
       *//* for(int i = 0 ; i<res.length;i++){
            res[i] = null;
        }*//*
        j = arrMin.length-1;
        for (int i = max_size-1; i>=0;i--){
            res[i+1] = (arrMax[i]+ arrMin[i])%10 + buff;
            if ((arrMax[i] + arrMin[i]) % 10 >= 0) {
                buff = 1;
            } else buff =0; res[i] = buff;


        }
        System.out.println(Arrays.toString(res));
    }*/
}
